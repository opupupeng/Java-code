package com.it.demo;

public class ForAndWhileAndDoWhileDemo {
    /**
     * 1.三种循环的区别
     * for循环：1.需要把对应的条件,计数器，计数器的累加放在一起
     *         2.先进行条件的判断在执行程序
     * while循环：1.需要一个判断的条件
     *           2.计数器需要在循环外边进行定义 计数器的累加需要定义在内部
     *           3.先进行条件判断，在执行程序
     * dowhile循环：1.需要一个判断的条件，判断条件是放在程序执行后边
     *             2.需要在do标签中进行计数器的累加
     *             3.先至少执行一次程序，再进行判断
     *break：终止循环
     */
}
